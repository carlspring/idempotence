# `idempotence-maven`

This Maven module is where the code for Maven extension is implemented.

## Directory Structure (Maven)

Under Maven, test resources are copied to the `target/test-resources/${testClass}/${testMethod}` directory by default.

See the [Configuration](../configuration.md) page for the system properties that control the base directory,
the separator, and other behaviour.
